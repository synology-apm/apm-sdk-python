[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "synology-apm-cli"
version = "0.2.1"
description = "Command-line interface for Synology ActiveProtect Manager, built on synology-apm-sdk"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Synology Inc." },
]
requires-python = ">=3.11"
keywords = ["synology", "activeprotect", "backup", "apm", "cli"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Systems Administration",
]
dependencies = [
    "synology-apm-sdk==0.2.1",
    "typer>=0.12,<0.26",
    "rich>=13.0",
]

[project.urls]
Homepage = "https://www.synology.com/products/ActiveProtectAppliance"
Repository = "https://github.com/synology-apm/apm-sdk-python"
Documentation = "https://synology-apm.github.io/apm-sdk-python/"
"Issue Tracker" = "https://github.com/synology-apm/apm-sdk-python/issues"

[project.scripts]
synology-apm-cli = "synology_apm.cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.uv.sources]
synology-apm-sdk = { workspace = true }
